<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/articles_list_layout"
    android:layout_width="match_parent" android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/articleLogo"
        android:layout_width="65sp"
        android:layout_height="65sp"
        android:scaleType="fitCenter"
        android:layout_alignParentLeft="true"
        android:layout_margin="5sp" />

    <TextView
        android:id="@+id/articleName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:hint="nom de l'article"
        android:textSize="18dp"
        android:textColor="@color/secondary_text"
        android:layout_toRightOf="@+id/articleLogo"/>

    <ImageButton
        android:id="@+id/articleDelete"
        android:layout_width="40sp"
        android:layout_height="65sp"
        android:scaleType="fitCenter"
        android:foreground="@color/icons"
        android:foregroundGravity="center_vertical"
        android:layout_alignParentRight="true"
        android:layout_margin="5sp"/>

    <TextView
        android:id="@+id/articlePriceQuantity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="18dp"
        android:textColor="@color/secondary_text"
        android:layout_below="@+id/articleName"
        android:layout_toEndOf="@+id/articleLogo" />
</RelativeLayout>